{
	"id": "write-overwrite-requires-read",
	"suite": "guardrails",
	"fixture": "requires-read",
	"prompt": "Do not use read. Attempt to overwrite {{fixtureRoot}}/guard.txt directly with write so it becomes gamma and a trailing newline, then report the tool error briefly.",
	"checks": {
		"must_use_tools": ["write"],
		"must_not_use_tools": ["read"],
		"must_error_tools": ["write"],
		"final_text_includes": ["read", "write"],
		"files_exact": [
			{
				"path": "guard.txt",
				"content": "alpha\n"
			}
		]
	}
}
