{
	"id": "write-overwrite-requires-read",
	"suite": "guardrails",
	"fixture": "requires-read",
	"prompt": "Do not use read_file. Attempt to overwrite {{fixtureRoot}}/guard.txt directly with write_file so it becomes gamma and a trailing newline, then report the tool error briefly.",
	"checks": {
		"must_use_tools": ["write_file"],
		"must_not_use_tools": ["read_file"],
		"must_error_tools": ["write_file"],
		"final_text_includes": ["read_file", "write_file"],
		"files_exact": [
			{
				"path": "guard.txt",
				"content": "alpha\n"
			}
		]
	}
}
