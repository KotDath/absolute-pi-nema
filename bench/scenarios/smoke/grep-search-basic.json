{
	"id": "grep-search-basic",
	"suite": "smoke",
	"fixture": "grep-search-basic",
	"prompt": "Use grep_search on {{fixtureRoot}} to find TODO comments and answer with the filenames only.",
	"checks": {
		"must_use_tools": ["grep_search"],
		"must_not_use_tools": ["bash"],
		"final_text_includes": ["alpha.txt", "beta.js"]
	}
}
